name: Publish Release

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Existing tag to publish, for example v1.1.3'
        required: true
        type: string

permissions:
  contents: write

jobs:
  prepare:
    name: Prepare Release Metadata
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.meta.outputs.tag }}
      version: ${{ steps.meta.outputs.version }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.inputs.tag || github.ref }}

      - name: Resolve version and release body
        id: meta
        shell: bash
        run: |
          set -euo pipefail

          if [[ "${GITHUB_EVENT_NAME}" == "workflow_dispatch" ]]; then
            TAG="${{ github.event.inputs.tag }}"
          else
            TAG="${GITHUB_REF_NAME}"
          fi

          VERSION="$(tr -d '\r\n' < build/version.txt)"
          EXPECTED_TAG="v$VERSION"

          if [[ "$TAG" != "$EXPECTED_TAG" ]]; then
            printf 'Tag %s does not match build/version.txt (%s)\n' "$TAG" "$VERSION" >&2
            exit 1
          fi

          NOTES_FILE="release-notes/$VERSION.md"
          BODY_FILE="release-body.md"
          : > "$BODY_FILE"

          if [[ -f "$NOTES_FILE" ]]; then
            cat "$NOTES_FILE" >> "$BODY_FILE"
            printf '\n\n' >> "$BODY_FILE"
          else
            printf '# CircuitSim %s\n\n' "$VERSION" >> "$BODY_FILE"
          fi

          {
            printf 'tag=%s\n' "$TAG"
            printf 'version=%s\n' "$VERSION"
          } >> "$GITHUB_OUTPUT"

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Build versioned jar asset
        run: |
          chmod +x ./scripts/build-jar.sh
          ./scripts/build-jar.sh

      - name: Upload release body
        uses: actions/upload-artifact@v4
        with:
          name: release-body
          path: release-body.md
          if-no-files-found: error

      - name: Upload jar asset
        uses: actions/upload-artifact@v4
        with:
          name: release-jar
          path: dist/CircuitSim-${{ steps.meta.outputs.version }}.jar
          if-no-files-found: error

  windows:
    name: Build Windows Release Assets
    runs-on: windows-latest
    needs: prepare

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.tag }}

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Install WiX Toolset v3
        shell: powershell
        run: |
          choco upgrade wixtoolset --no-progress -y
          $wixBin = 'C:\Program Files (x86)\WiX Toolset v3.14\bin'
          if (-not (Test-Path $wixBin)) {
            throw "WiX bin directory not found at $wixBin"
          }
          Add-Content -Path $env:GITHUB_PATH -Value $wixBin

      - name: Build Windows packages
        shell: powershell
        run: |
          powershell -ExecutionPolicy Bypass -File .\scripts\package-windows.ps1

      - name: Archive Windows portable app
        shell: powershell
        run: |
          $version = '${{ needs.prepare.outputs.version }}'
          $portableZip = "build/package/windows/CircuitSim-windows-portable-$version.zip"
          if (Test-Path $portableZip) {
            Remove-Item $portableZip -Force
          }
          Compress-Archive -Path build/package/windows/CircuitSim\* -DestinationPath $portableZip

      - name: Upload Windows release assets
        uses: actions/upload-artifact@v4
        with:
          name: release-windows
          path: |
            build/package/windows/CircuitSim-*.exe
            build/package/windows/CircuitSim-windows-portable-*.zip
          if-no-files-found: error

  linux:
    name: Build Linux Release Assets (${{ matrix.release_arch }})
    runs-on: ${{ matrix.runner }}
    needs: prepare
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-latest
            release_arch: x86_64
            deb_arch: amd64
            rpm_arch: x86_64
          - runner: ubuntu-24.04-arm
            release_arch: arm64
            deb_arch: arm64
            rpm_arch: aarch64

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.tag }}

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Install Linux packaging dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y fakeroot rpm flatpak flatpak-builder
          sudo flatpak remote-add --system --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo
          sudo flatpak install --system -y flathub org.freedesktop.Platform//24.08 org.freedesktop.Sdk//24.08 org.freedesktop.Sdk.Extension.openjdk21//24.08

      - name: Build Linux packages
        run: |
          chmod +x ./scripts/build-jar.sh ./scripts/package-linux.sh ./scripts/package-flatpak.sh ./scripts/run-dev.sh
          ./scripts/package-linux.sh
          ./scripts/package-flatpak.sh

      - name: Archive Linux app image
        run: |
          version='${{ needs.prepare.outputs.version }}'
          tar -C build/package/linux -czf "build/package/linux/CircuitSim-linux-app-image-$version-${{ matrix.release_arch }}.tar.gz" CircuitSim

      - name: Upload Linux release assets
        uses: actions/upload-artifact@v4
        with:
          name: release-linux-${{ matrix.release_arch }}
          path: |
            build/package/linux/circuitsim_*_${{ matrix.deb_arch }}.deb
            build/package/linux/circuitsim-*.${{ matrix.rpm_arch }}.rpm
            build/package/linux/CircuitSim-linux-portable-*-${{ matrix.release_arch }}.tar.gz
            build/package/linux/CircuitSim-linux-app-image-*-${{ matrix.release_arch }}.tar.gz
            build/package/flatpak/CircuitSim-linux-*-${{ matrix.release_arch }}.flatpak
          if-no-files-found: error

  publish:
    name: Publish GitHub Release
    runs-on: ubuntu-latest
    needs:
      - prepare
      - windows
      - linux

    steps:
      - name: Download release body
        uses: actions/download-artifact@v4
        with:
          name: release-body
          path: release-body

      - name: Download Windows assets
        uses: actions/download-artifact@v4
        with:
          name: release-windows
          path: release-assets/windows

      - name: Download Linux assets
        uses: actions/download-artifact@v4
        with:
          pattern: release-linux-*
          path: release-assets/linux
          merge-multiple: true

      - name: Download jar asset
        uses: actions/download-artifact@v4
        with:
          name: release-jar
          path: release-assets/jar

      - name: Publish release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.prepare.outputs.tag }}
          name: CircuitSim ${{ needs.prepare.outputs.version }}
          body_path: release-body/release-body.md
          files: |
            release-assets/windows/**/*
            release-assets/linux/**/*
            release-assets/jar/**/*
          fail_on_unmatched_files: true
